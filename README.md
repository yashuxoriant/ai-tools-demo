#1. Software Development Lifecycle (SDLC)
Requirements → spec generation (what we just did with OpenSpec)
Code review automation (style, security, logic checks on PRs)
Automated test generation (unit/integration/e2e) from specs or code diffs
Commit message / PR description generation from diffs
Dependency/version upgrade automation (bump + test + PR)
Dead code / unused import detection & cleanup
Automated changelog / release notes generation
Boilerplate/scaffolding generation for new modules/services ai-tools-demo
